## Quick Wins — Next Up

Based on where BookSuite is now (Stripe Connect live, emails wired, i18n/currency switcher in, security hardened), here's a batch of small, high-impact tasks we can knock out fast before starting the big Growth/SEO/Onboarding push.

### 1. SEO basics (10 min)
- Run an SEO scan on `booksuite.online` and fix the low-hanging findings (title, meta description, canonical, og tags, JSON-LD for SoftwareApplication).
- Add `robots.txt` + `sitemap.xml` with real routes.
- Verify domain in Google Search Console.

### 2. Landing page polish (15 min)
- Add a "Trusted by" / logo strip placeholder.
- Add a sticky CTA on mobile ("Start free trial").
- Add Open Graph preview image so link shares look professional.

### 3. Onboarding nudges (15 min)
- Add a checklist card on the dashboard: ✅ Verify email · ✅ Connect Stripe · ✅ Add first employee · ✅ Embed widget.
- Auto-hide when all done.

### 4. Trust & Legal pages (10 min)
- Add `/privacy`, `/terms`, `/security` pages (linked from footer). Required for Stripe, Google, and app-store trust.

### 5. Analytics (5 min)
- Add a lightweight event tracker (page views, signup, stripe_connected, first_booking) so we can measure the growth work later.

### 6. Small UX fixes
- Loading states on Stripe connect button.
- Empty states for Bookings/Clients/Staff/Shifts when brand new.
- Toast on successful language/currency switch.

---

### After the quick wins
Then we move into the **real growth phase**:
- **A. SEO/Content** — blog engine, programmatic pages ("Booking software for [industry]"), backlinks.
- **B. Full i18n** — translate dashboard + emails, not just landing.
- **C. Retention** — weekly digest email, referral program, in-app tips.

---

**Suggested order for this turn:** knock out **1 → 4 → 3 → 2 → 5 → 6** in that sequence (SEO + legal unlock everything else). Want me to start with #1 (SEO scan + meta tags + sitemap + robots + GSC verification) as the first build step?
